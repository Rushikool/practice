---
 - hosts: all
   remote_user: rushikesh
   become: true
   tasks:
           - name: adding the user named ansuser
             user:
                     name: hash
                     state: present
                     
                  
